import { Poppins, Nanum_Gothic } from 'next/font/google';

export const poppins = Poppins({ weight: '400', subsets: ['latin']});
export const nanum = Nanum_Gothic({ weight: '400', subsets: ['latin']});

export const black_poppins = Poppins({ weight: '900', subsets: ['latin']});